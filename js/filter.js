'use strict';

(function () {
  var mapFilter = document.querySelector('.map__filters');
  var filterType = mapFilter.querySelector('#housing-quests');
  var filterPrice = mapFilter.querySelector('#housing-price');
  var filterRooms = mapFilter.querySelector('housing-rooms');
  var filterGuests = mapFilter.querySelector('housing-guests');
  var filterFeatures = mapFilter.querySelector('#housing-features');

  var pins = [];
  var filteredPins = [];

  var

  var updatePins = window.util.debounce(function () {

  });
})();
